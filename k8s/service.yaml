apiVersion: v1
kind: Service
metadata:
  name: fraud-detection-service
spec:
  type: LoadBalancer
  selector:
    app: fraud-detection
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000  # BentoML server
    - protocol: TCP
      port: 81
      targetPort: 5000  # MLflow UI
